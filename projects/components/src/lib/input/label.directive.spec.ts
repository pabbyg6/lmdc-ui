import { LmdcLabel } from './label.directive';

describe('LmdcLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new LmdcLabel();
    expect(directive).toBeTruthy();
  });
});
